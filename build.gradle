// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    ext {
        compileSdkVersion = 30
        minSdkVersion = 21
        targetSdkVersion = 30

        // App dependencies
        kotlinVersion = '1.4.32'  // Strange crashes with 1.5.0
        hiltVersion = '2.35.1'

        // Local variables (use def)
        activityVersion = '1.3.0-alpha07'
        androidxCoreVersion = '1.6.0-alpha02'
        constraintLayoutVersion = '2.1.0-beta01'
        coroutinesVersion = '1.5.0-RC-native-mt'
        fragmentVersion = '1.3.3'
        lifecycleVersion = '2.3.1'
        materialVersion = '1.3.0'
        preferenceVersion = '1.1.1'
        recyclerviewVersion = '1.2.0'
        roomVersion = '2.4.0-alpha01'
        sentryVersion = '5.0.0-beta.1'
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:4.2.0-rc01'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"
        classpath "io.sentry:sentry-android-gradle-plugin:1.7.36"
        classpath "com.google.dagger:hilt-android-gradle-plugin:$hiltVersion"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

ext {
    arch_version = "2.1.0"
}